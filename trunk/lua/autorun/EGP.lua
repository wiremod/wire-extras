if not EGP then
	include("EGP/EGP_Shared.lua")
	AddCSLuaFile("EGP/EGP_Shared.lua")
end
--This should be done by E2 with a implemented code.
--But incase it fucks up then this will make sure the lib is up and running.